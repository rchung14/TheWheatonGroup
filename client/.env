# Build-time flags only — never put secrets here; CRA inlines everything
# in this file into the public client bundle.

# Don't ship source maps: they expose original source and file paths.
GENERATE_SOURCEMAP=false
